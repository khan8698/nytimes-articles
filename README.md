# NY Times Most Popular Articles

## Description
This project is a simple React app to display NY Times most popular articles. It fetches data from the NY Times API and displays a list of articles, with details shown when an item is clicked.

## Setup
1. Clone the repository
2. Run `npm install` to install dependencies
3. Create a `.env` file in the root directory with the following content:
4. Run the app using `npm start`

## Testing
- Run unit tests with Jest: `npm test`
- Run UI tests with Cypress: `npm run cypress`

## Building
- Build the project: `npm run build`

## Linting
- Run ESLint: `npm run lint`
